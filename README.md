This project consist of simple implementations of linear and logistic regression models using TensoFLow library.

It also have simple implmentation of multi layer perceptron and convolution neural networks based on MNIST data using TensorFlow library.
